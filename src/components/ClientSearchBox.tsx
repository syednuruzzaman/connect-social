"use client";

import SearchBox from "./SearchBox";

const ClientSearchBox = () => {
  return <SearchBox className="flex-1 max-w-md" placeholder="Search Connect..." />;
};

export default ClientSearchBox;
